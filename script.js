/* ======================================
   LIENS Z
   Main JavaScript
====================================== */

document.addEventListener("DOMContentLoaded", () => {

    initSideMenu();

    initSlider();

    initSearch();

    initCategories();

    initFavorites();

    initCart();

});
